language: python

python:
  - "3.5"
  - "3.6"
  - "3.7"


install:
  - travis_retry pip install --upgrade pip setuptools
  - travis_retry pip install --upgrade tox-travis
  - python setup.py bdist_wheel
  - pip install ./dist/torn-*.whl
  - travis_retry pip install --upgrade tox

script:
 - tox --installpkg ./dist/torn-*.whl

notifications:
  email:
    - shubhodeep9@gmail.com